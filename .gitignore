# credenciais / dados sensíveis — nunca versionar
*token*
*.env
.env*
secrets*
*.key
# JSON de credencial que o Google Cloud Console entrega ao criar o OAuth client
client_secret*.json
*credentials*.json
# dados brutos com PII de clientes
data.json
sales_full.json
mkt_rows.json
raw/

# config local do Claude Code
.claude/
